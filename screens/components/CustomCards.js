import { View, Text, Modal, TouchableOpacity, Pressable } from 'react-native'
import React from 'react'

const CustomCards = ({day}) => {
  return (
    <Text>{day}</Text>
  )
}

export default CustomCards